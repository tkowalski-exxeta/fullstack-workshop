query GetFormMain {
  forms {
    _id
    title
  }
}

mutation CreateForm($formInput: FormInput!) {
  createForm(form: $formInput) {
    _id
    title
  }
}

mutation DeleteForm($formId: ID!) {
  deleteForm(formId: $formId)
}
