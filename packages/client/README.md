# 03-React

# Easy Route
- Create a react-ts app using 'npm create vite@latest' (already done)
- Write the basic layout using CSS flex / grid (Layout and Listview)
- Extract a Layout Component
- Add functionality to change to the details view if a Form gets seleted/clicked in the Listview
   - use Props & Callbacks as well as React-Hooks like e.g. useState, useEffect
- Display FormDetailView

# Advanced (Addon)
- Improve the Layout / UX
- In of using 'formService' implement make real calls to a REST-API
   - API are implemented using MockServiceWorker
   - Queries are made using TanStack Query with 'fetch'

